# HW08-09 – PyTorch MLP: регуляризация и оптимизация обучения

## 1. Кратко: что сделано

- Выбран датасет C: `CIFAR10`. Хоть и MLP работает хуже, но было интересно поработать с rgb батчами
- В части A мы строили MLP модели и добавляли к ней регуляризацию (Dropout), нормализацию активации (BatchNorm) и EasyStopping, функцию не позволяющая переобучаться модели.
- В части B оптимизировали лучшую конфигурацию, выявленную в части A, с помощью LR, оптимизаторов SGD+momentum, weight decay.

## 2. Среда и воспроизводимость

- Python: 3.12
- torch / torchvision:
- Устройство: CPU
- Seed: 42
- Как запустить: открыть `HW08-09.ipynb` и выполнить Run All.

## 3. Данные

- Датасет: `CIFAR10`
- Разделение: train split 80/20 + test из torchvision
- Трансформации (transform): ToTensor + Normalize
- Комментарий: что важно в данных (число классов, размерность, сложность)
    - Число классов: 10, 
    - размерность батча: 64

## 4. Базовая модель и обучение

- Модель MLP (кратко): 
    - число скрытых слоёв (hidden_dims): 256-128, 
    - размеры слоёв (input_dim): 3072, 
    - активация: relu
- Loss: CrossEntropyLoss
- Базовый Optimizer (для части A): Adam (lr=1e-3)
- Batch size: 64
- Epochs (макс): 20
- EarlyStopping: (patience=5, metric=val_accuracy и val_loss)

## 5. Часть A (S08): регуляризация (E1-E4)

Опишите, что меняли. Формулировки должны быть короткими и сопоставимыми.

- E1 (base): 2 скрытых слоя, без Dropout и без BatchNorm
- E2 (Dropout): как E1 + Dropout(p=0.3)
- E3 (BatchNorm): как E1 + BatchNorm
- E4 (EarlyStopping): E3 + EarlyStopping

## 6. Часть B (S09): LR, оптимизаторы, weight decay (O1-O3)

- O1: LR слишком большой (Adam, lr=1e-1)
- O2: LR слишком маленький (Adam, lr=1e-5)
- O3: SGD+momentum (momentum=0.9) + weight_decay=1e-4 (lr=0.1)

## 7. Результаты

Ссылки на файлы в репозитории:

- Таблица результатов: `./artifacts/runs.csv`
- Лучшая модель: `./artifacts/best_model.pt`
- Конфиг лучшей модели: `./artifacts/best_config.json`
- Кривые лучшего прогона: `./artifacts/figures/curves_best.png`
- Кривые “плохих LR”: `./artifacts/figures/curves_lr_extremes.png`

Короткая сводка:

- Лучший эксперимент части A оказался E4
- Лучшая val_accuracy: 0.554
- Итоговая test_accuracy (для лучшей модели): 0.5382
- Что видно на O1 (слишком большой LR): на графике при большом LR наблюдаются скачки, расходимость.
- Что видно на O2 (слишком маленький LR): при низком LR на графике видно слишком долгое обучение.
- Как повёл себя O3 (SGD+momentum + weight decay) относительно Adam (по кривым/метрике):

## 8. Анализ

- Где на графиках видно переобучение (если видно) и как его изменили Dropout/BatchNorm.
    - на модели E1 видно, что переобучение происходит на 6-7 эхопе, при Dropout переобучение происходит на 10 эпохе.
- Что сделал EarlyStopping (на какой эпохе остановил, помог ли).
    - EarlyStopping действительно помог при обучении модели E4, обучение остановилось на 17 эпохе из 20.
- Почему O1 и O2 “плохие” (что конкретно видно по loss/accuracy).
    - основная проблема O1 и O2 - неэффективное обучение
    - при маленьком lr loss падает очень медленно, acc почти не растёт
    - при большом lr loss и acc нестабильны, скачет или растёт
- Что даёт SGD+momentum и зачем добавляют weight decay (что заметили в ваших результатах).
    - SGD+momentum ускоряют сходимость, сглаживают шум градиентов.
    - weight decay уменьшает overfitting
- Почему выбранный лучший конфиг разумен именно для этого датасета.
    - Отобраный конфиг модели E4+O3 имеет наилучшие метрики по loss/accurancy 

## 9. Итоговый вывод

В ходе работы была исследована и реализована MLP-модель. Базовая модель исходя из графиков переобучалась с увеличением эпох обучения. С помощью Dropout и BatchNorm модель стала стабильнее. Инструмент EarlyStopping позволил остановить обучение в момент наилучшего качества на валидационной выборке. Эксперименты с learning rate показали, что слишком большой шаг приводит к нестабильному обучению, а слишком маленький к очень медленной сходимости. Также было показано, что SGD с momentum и weight decay ускоряет обучение и уменьшает переобучение.

## 10. Приложение (опционально)

Если вы делали дополнительные сравнения:

- чистое сравнение Adam vs SGD на одном lr без weight decay
- другая активация / инициализация
- дополнительные графики: `./artifacts/figures/...`